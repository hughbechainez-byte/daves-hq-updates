# Dave's HQ Update Site

Host this folder with GitHub Pages, Cloudflare Pages, or any static host.

The app should check:

`https://YOUR_HOST/updates.json`

For GitHub Pages, a typical URL is:

`https://YOUR_GITHUB_USERNAME.github.io/daves-hq-updates/updates.json`

Update `updates.json` for every new release.
